requests:
  default:
    method: "GET"
    path: "/test-request"
responses:
  default:
    condition: "test == :value"
    status: 200
  test_body:
    condition: "test == :value"
    body: "test_request"
    transitions:
      test: "another value"
